# packaging/

Distribution scaffolding for the public identity **memtrials**. Filled by the
`PACKAGE` move much later (Phase 5+), once the engine has ascended far enough to
be worth shipping.

Empty at Phase 0. Whatever lands here must still obey the whole constitution —
zero third-party dependencies, deterministic, `trials/run.py` green.
