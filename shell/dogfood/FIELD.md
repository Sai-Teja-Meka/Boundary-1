# FIELD.md — what chafed, in use

The dogfood log. `README.md` says what the adapter decided; this file says what
it felt like to use, and it is only useful if it is honest. A note goes in when
something rubbed — a cue that should have worked and didn't, an operation that
needed a workaround, a field that could not be filled truthfully. Praise is not a
field note.

## Standing format

One entry per line, three fields, `|`-separated:

```
<YYYY-MM-DD> | <cue or operation> | <what chafed — one line>
```

The date is the session's date. The second field is the exact cue as typed
(`recall anchors l1`) or the operation (`backfill`, `remember`, `status`).
The third is one line: what chafed, not what to do about it. Entries are
append-only and in date order; nothing here is ever rewritten.

## Notes

2026-07-24 | backfill (13 lines) | `open_questions` is empty on all 13 backfilled events — the log records what a session decided and never what it left open, so the memory of the project's first eleven sessions is structurally thinner than every session after it, and no parser can fix that.
2026-07-24 | backfill (L1 FORGE, L2 ASCEND) | The two sessions that actually built the engine have the tersest log lines in the file — `retention` and `recall` — so they backfill to one decision and zero files each: the store's thinnest entries are its most important ones.
2026-07-24 | recall anchors l1 | Abstained as ambiguous across 3 events, and rightly, but `anchors` turns out to have df=13 — every log line ends with `<anchors: …>` metadata, so the log's own bookkeeping manufactures a token that is present everywhere and discriminates nothing.
2026-07-24 | recall humility ceiling | The cue I expected to hit found nothing: `ceiling` is a `BOUNDARY.md` word, not a `BOUNDARY.log` word, and the store only knows what the log wrote down — the memory is exactly as wide as its source, which is narrower than the project.
2026-07-24 | remember (schema design) | The cue surface had to be an object with the constant value `1`; a list (`["a","b"]`) index-qualifies to `tok.0=a` and a count (`3`) makes a different atom than a cue's `1` — the engine's flattener dictated the schema rather than the other way round, which is correct and still felt like being told.
2026-07-24 | backfill (files_touched) | Mechanical path extraction turned "grounded on README/templates.py:80" into the file `README/templates.py`, which does not exist — a derivation that is faithful to the line and wrong about the world, and the store cannot tell the difference.
2026-07-24 | backfill (decisions) | Splitting prose on `;` had to be taught about parentheses after the memoryagentbench line split mid-`(…)`; every clause is still a guess at what a "decision" was, and the weakest thing in the store is the field that sounds most authoritative.
2026-07-24 | remember (this session's live event) | Writing the observation down made it false: the live event's `open_questions` quote the cue `anchors l1`, so df(`anchors`) went 13 → 14 and the cue now matches 4 events instead of the 3 the note claims — a memory that records its own recall behaviour perturbs it, and the entry stays exactly as it was written.
2026-07-24 | remember --move | `move` cannot be validated against the §9.1 move set, because the log itself carries `FORGE-CORRECTION` and `THEORY`, which that set does not list — the constitution's vocabulary and the log's vocabulary have already drifted, at 13 lines.
2026-07-25 | recall writ | Abstained as ambiguous across 2 events — the GAPMAP that flagged WRIT unverified and the AUTOPSY that verified it — because the store has no notion of a session's SUBJECT: an autopsy's target lives only inside prose decisions, so the memory cannot tell the session ABOUT WRIT from a session that merely mentioned it, and writing the definitive event down is precisely what took df(writ) from 1 to 2 and destroyed the cue that would have found it.
2026-07-25 | remember --file | The eleven backfilled events record the bare path `ANATOMY.md` because the log lines only ever said "ANATOMY.md deposited", while this session passed the real `autopsy/writ/ANATOMY.md`; the store now holds two incompatible conventions for the same artifact and `recall` cannot join them, so the six earlier autopsies share one file that belongs to none of them.
2026-07-26 | remember --decision | The flag form could not carry this session's prose through the shell at all — parentheses and apostrophes in a decision broke the command line twice — so the summary was assembled as JSON by a throwaway script and piped in; the store's own write path turns out to be easiest to use from a program, which is the opposite of the case it was designed for.
2026-07-26 | recall form b | Abstained on df(form)=6 because the normalizer keeps only runs of two characters or more, so `Form B` — the name of this session's own thesis trial — normalizes to the single token `form`: the one character that discriminates it is exactly the one the tokenizer cannot keep, and no amount of writing it down will make it findable.
2026-07-26 | status | The store is still `layer_cap 2` at 0 permille of a 16 MB cap, so the project's memory is held by a two-layer engine while the engine it remembers has four — the layer this session built is the one its own history does not use, and nothing in the shell notices or is supposed to.
2026-07-26 | remember --json | The summary schema rejected a `cues` field outright: the cue surface is *derived* from the prose fields and cannot be authored, so a session can only be found later by words it already happened to write in a decision — this entry's own most distinctive word, `preview`, is findable purely by accident of phrasing, and there is no way to say "index me under this" without padding the prose to do it.
2026-07-26 | status (the t/log-line drift) | The store could not tell me it had drifted. Finding the unremembered Stage-A session meant diffing 23 log lines against 22 events by hand and matching on `log_line` prefixes, because an event records the line's *text* and never which line it was — the store knows what it was told and has no idea what it was not told, which is the honest limit and still the thing I actually needed.
2026-07-26 | consolidate --budget 8000 (THE DEMOTION SEAM, measured out-of-suite) | Asked whether a demoted session summary is still reachable by `recall <cue>`, and the honest answer refutes the question: **no session summary is ever demoted.** `python3 -m shell.dogfood consolidate --budget 8000` — at the default 4194304-unit cap the fold is 224 derived events, 0 demotions, 23/23 summaries reachable by cue; at 8000 it is 189 demotions and 15 losses, with 209 of 224 events still answered by `read(t)` (20 still episodes, 189 regenerated from the chains) and `recall` reaching at most those 20 — so a demoted episode keeps its content and loses its cue, exactly as README-l4 §4 says in prose and nowhere in a number. The 189 are all derived *assertions*; a session summary is irreducible to the frozen facet map, so pressure FORGETS it instead (15 of 23 at 8000, 22 of 23 at 3000, demoted = 0 at every cap tried), and a forgotten summary is unreachable by both channels. Probed directly rather than inferred: the cue `{"kind":"attr","entity":1,"key":"suite","val":"98/98 green"}` answers at the roomy cap and abstains at 8000 while `read(t)` still returns that assertion byte-exactly.
2026-07-26 | consolidate (the reading) | A session cannot be its own assertion, so one event per session had to become one episode plus N assertions in the derived stream: `facet` reads any payload carrying `kind/entity/key/val`, but `invertible` compares CANONICAL BYTES against the four-field rebuild, so attaching the summary's prose to its own assertion costs the inversion — the engine's frozen grammar dictated the shape of the reading exactly as the flattener once dictated `tok`, and this time the thing it dictated was the event count.
2026-07-26 | consolidate (the atlas-None seam) | Measured while proving the above, and it points the other way: a payload that reads as an assertion but does NOT invert sets `atlas[key] = None`, and when the budget then releases its episode the engine books it as a **demotion** while `read(t)` abstains on it forever — 8 such writes at a 40-cell cap gave demotions 7, forgetting-record count 0, `read(0)` None, so the ledger says nothing was lost and the content is gone. Every frozen corpus has zero non-invertible keys (murk 18 atlas keys, chronicle 17, l4stream 16, all `None`-free), so no trial reaches this path; the closing-ledger identity of README-l4 §2.3 is true of the corpora it was measured on and not of the code in general. Design input for a strain trial, nothing more — no frozen file touched, no claim made about a gate.
2026-07-26 | consolidate (log_line drift) | The derived view can only assert what the line it was handed carries, and two sessions handed it less than the log has: store `t=20` ends at `<suite: 176 green / 11 skipped>` with the `<anchors: …>` tag cut off, and `t=21` — the session that CLAIMED LAYER 4 — ends mid-sentence at "…far below." carrying neither tag, so the project's own ascension asserts no suite and no anchors while `BOUNDARY.log` line 23 states both. README.md says `log_line` is verbatim; it is verbatim about what it was given, and nothing checks that against the file.
2026-07-26 | consolidate (open_question) | The engine's only multi-value structure is a supersession chain, and open questions are an append log, not a supersession: 44 of them ride the one `(entity, open_question)` chain and come back only by walking `asof` over every `t` and collecting distinct supports, and nothing in the state distinguishes a question that is still open from one the next assertion superseded — the store can say what was asked and never what is still being asked.
2026-07-26 | consolidate (entity ids) | The reading had to invent integers: `facet` refuses a non-integer entity and `_actor` will not attribute an episode to one, so `boundary-1-memory` is entity `1` purely because it appeared first in the store, and the identifier that makes the whole per-project fold work is the one thing in the view that is not in the store.
2026-07-26 | status (still layer_cap 2, now on purpose) | The chafe recorded on 2026-07-26 is answered and deliberately only half-answered: the store can now be *asked* to consolidate but still is not consolidated, because writing the derived assertions back would put an inferred fact and a remembered one side by side in the same ledger — the mem0 defect the autopsy names and the L7 self-pollution law forbids — so the derivation is recomputed every time and thrown away, and `status` will keep printing `layer_cap 2` until there is a reason better than symmetry.
2026-07-26 | consolidate --budget 8000 (after remembering it) | The store perturbed the measurement again, and this time it moved the numbers the note above records: remembering this session took the fold from 224 derived events to 238 and the same command now reports 201 demotions, 17 losses and 7 of 24 summaries surviving, against the 189 / 15 / 8 of 23 measured before the write — every conclusion holds (demoted = 0 for summaries, cue channel reaching only the held episodes) and every figure moved, so the entry above stays exactly as it was written and this one carries the drift, because a note whose numbers are re-derived on each read is not a field note.
2026-07-26 | recall demotion invariant | The session that CLOSED the seam is unfindable by the words that name it: `demotion invariant` abstains across 3 events, `demotion seam` across 5, `blocking` and `atlas` across 3 each — and in every case the collisions are the DOGFOOD whose field note REPORTED the defect and the PULSE that RECOMMENDED the strain, so the store cannot tell the session that named a problem from the session that fixed it; the 2026-07-25 `writ` note found the store has no notion of a session's SUBJECT, and a defect's lifecycle turns out to be that same blind spot seen along time instead of across topics — every honest write about a defect makes the defect's own name worse as a cue, and the fix is the third write.
2026-08-01 | intend (the schema) | An intention cannot carry a cue surface, and the rule that forbids it is the one that makes it work: `README-l5 §1.2` arms a payload only if it rebuilds from `(iid, cond, fire)` as CANONICAL BYTES, so adding `tok` — the exact field `remember` adds to every session summary to make it findable — leaves the payload perfectly canonical and stops it arming (asserted in t_intend.py). So the store's newest kind of event is the one kind of event `recall` can never reach: a promise is t-addressable and not cue-addressable, one tier past the 2026-07-26 demotion seam, and this time it is not the budget that closed the channel but the invertibility rule that opens the capability at all.
2026-08-01 | intend --project (the entity id) | The 2026-07-26 note that the reading had to INVENT integer entity ids gets worse when a promise uses one: `boundary-1-memory` is entity 1 only because it appeared first in the store, and the condition freezes that 1 into an immutable event. The derived view re-derives the mapping on every read and is therefore self-correcting; a stored intention is not, so if this store ever holds a project whose first appearance changes the numbering, every promise already declared would go on watching the number and not the project. Nothing in the store records which project entity 1 was — the identifier that makes the whole fold work is still the one thing that is not in it.
2026-08-01 | intend + consolidate (THE CONSOLIDATION BOUNDARY, measured out-of-suite) | The mandatory measurement, on a copy of the real store: declared `count(attr)>=300` guarded by `kind=attr` at store t=32, remembered one more session (12 open questions) to push the fold past the threshold, then replayed at four caps. At the roomy 4194304 the fold fires at derived t=334 on the write from store t=33; at 12000 the same firing survives 282 demotions and 19 losses with 43 episodes held (29 of them `attr`); at 7000 it survives 305 demotions and 25 losses with 6 `attr` episodes left of the 309 the counter says it counted — the fold outlives the episodes it counts, which is `README-l5 §2.3` on this project's own history rather than on l5stream. **Exactly once at every cap**: the §7.1 `fired` ledger answers with a one-element list, and `next_t − |caller stream|` = 344 − 343 = 1 agrees with it. **The t-accounting matches R6 clause 2 exactly**: of 343 caller writes precisely one advanced `next_t` by more than 1, and it advanced it by 2 — `1 + f` with `f = 1` — and it was the `attr` assertion, not the session episode it came from. The unexpected half is the promise's OWN episode: at 8000 and above it is taken back and `read(t0)` returns the `intend` event byte-exact; at 7000 there is no room and it is booked into the forgetting record, so `read(t0)` abstains — while the still-PENDING seed intention beside it stays readable byte-exact at the same cap, because a pending entry regenerates its event and a fired one no longer can. A promise is readable exactly as long as it is unkept.
2026-08-01 | consolidate (a firing is not in the ledger) | The store can hold a promise and cannot keep one: the state file is `layer_cap 2`, so arming, watching and firing all happen inside the derived replay that `consolidate` throws away. A firing is therefore a FACT ABOUT THE STORE, re-derived identically on every read and never a row — which is the honest consequence of the 2026-07-26 refusal to write derivations back, and is also why `demotions 1` now appears at a cap under no pressure at all: arming releases the intention's episode at the door. The report had to learn to say which demotions are the budget's and which are the layer's, because the number alone stopped meaning what it meant one layer ago.
2026-08-02 | remember (THE ELEVEN-MOVE-OLD PROMISE, KEPT) | The store kept a promise nobody had touched in eleven moves, and it kept it on the write that made it true. `iid 1` was armed at store `t=31` on 2026-08-01 with the condition `kind=attr and entity=1 and key=layer and val>=6`, carrying the `INTERFACE.md` attribute-gap reminder from `[L4] [PACKAGE]`; this session's `remember` is the first summary this project has ever written whose `log_line` opens `[L6]`, so `layer_of` read 6, the derived stream asserted `{kind:attr, entity:1, key:layer, val:6}`, and the intention FIRED — once, at store `t=39`, derived `t=397`. Measured on a COPY of the store before the real one was written, and identical on it, which is the only way to report a firing without the report being the thing that caused it (the 2026-07-24 observation-perturbs-the-observed note, finally routed around rather than suffered). **The t-accounting is `R6` clause 2 of an ENGINE on real data**: of 406 derived caller writes exactly ONE advanced `next_t` by more than 1 — the `attr` assertion at derived `t=396`, whose transition emitted the firing at `t=397` and left `next_t` at 398, `1 + f` with `f = 1` — and it was the assertion, not the session episode it came from, exactly as the 2026-08-01 out-of-suite measurement predicted it would be. `{op:fired,iid:1}` answers with a ONE-ELEMENT list, `read(397)` returns the emitted payload byte-exact, the pending set is empty and the fired ledger holds 1. **And the payload was right about the session it arrived in and wrong about the order.** It said *"Layer 6 is where the next shared scorer is written … so state the contract before the sixth scorer assumes it"*, and `trials/_l6score.py` IS that scorer — but it was written at Stage B, one session before the promise could fire, so the contract is being stated AFTER the assumption. A condition over a *layer* fires when the layer is CLAIMED, and a layer's scorer is written when its trials are, which is a stage earlier: the store can watch for a fact and cannot watch for the moment before it. `trials/adapters/README.md` now carries the note the promise asked for, one session late, with the lateness recorded in it — plus `state.next_t`, which the fifth and sixth scorers both read as an attribute and which the original reminder did not know to name. **And the count in this note moved while the note was being written**, for the third time in this file: the remember had to be redone after the ritual write turned a dogfood trial red, the corrected summary carried one more decision and one more file, and 404 derived caller writes became 406 — the firing's own three numbers (396, 397, 398) did not move, because they are pinned by the assertion that satisfies the condition and not by how much prose the session wrote around it. The 2026-07-24 and 2026-07-26 notes recorded this as a hazard; here it is a reassurance, and the difference is which numbers a claim rests on.
2026-08-02 | consolidate (THE CALIBRATION CENSUS, measured out-of-suite) | The mandatory measurement, on a copy of the real store with this session's own `intend` and `remember` already applied (42 stored events, 420 derived), and it comes back a **negative that had to be measured to be worth anything**. Census at entity 1, one row per key the reading asserts plus every key the ENGINE calls set-once: `layer` 40 assertions / 7 distinct claimants, `move` 40/12, `suite` 33/21, `anchors` 30/6, `decisions` 40/13, `files_touched` 40/14, `open_questions` 40/6, `open_question` 114/114 — and every single one states **1000‰**, with `{"op":"calibration"}` reporting **ties 0**. The finding is NOT that nothing changed; almost everything changed, repeatedly. It is that **not one of those disagreements is a CONTRADICTION**: a chain that disagrees with itself is a contradiction only where the key admits exactly one value, the engine's frozen reading calls exactly one key set-once (`origin`, `corpora/murk`'s own set-once attribute), and no session summary states an origin — so the shell's declared set-once set is EMPTY (computed as the intersection, so it cannot widen), `permille(1/d)` has nothing in this project's history to price, and **the project's past contains no contradiction under the declared reading.** The emptiness is a row and not a silence: the census asks about `origin` anyway — those are the only keys a confidence can move on — and prints its abstention. And 1000‰ is a proof rather than a default, because the same renderer prices a set-once tie at 500‰ and an as-of *before* the contradiction at 1000‰ on a fixture. The sharpest part is the fact I went looking for and could not honestly assert: the one thing about this store that IS set-once is a project entity's identity, and asserting it would be worthless, because `project_entities` mints the entity id FROM that value, so `d = 1` would be a theorem of the reading and not a measurement of the history. **The store cannot buy itself a contradiction by declaring one** — what it can be uncertain about is bounded by what its reading can assert, which is the 2026-07-24 "exactly as wide as its source" note arriving at the layer that measures certainty.
2026-08-02 | consolidate (THE FIRED LEDGER AFTER IID 1, and a seam the firing left in the report) | With one promise kept and one newly armed the ledger reads **1 pending, 1 fired (26 + 7 cells)**, and the two tiers point OPPOSITE WAYS through the same `read(t0)` — which is the take-back rule visible in the *surface* and not only in the engine (`README-l5 §1.3`): iid 1's own `intend` event comes back byte-exact tagged **`recall`** at 1000‰, because nothing regenerates a fired intention's episode once the pending entry is gone, so `recall` is the only honest tag it can carry and `derive` there would mean the engine believed it could rebuild something it cannot; iid 2's comes back byte-exact tagged **`derive`**, regenerated by the pending entry that is still holding it. The fired event itself (derived `t=397`) comes back `derive` from the ledger that regenerates it, and of 420 derived events exactly **one caller write advanced `next_t` by more than one** — the `attr` assertion at derived `t=396`, `1 + f` with `f = 1`, `R6` clause 2 of an engine on real data for the second session running. **And the firing left a seam in the report, found by reading it rather than by measuring anything**: the line `of which N are ARMED INTENTIONS … — not pressure` was written when arming was the only demotion at the door, so with `pending 0` and `demotions 1` it attributed nothing and left a demotion looking like the budget's work at a cap under no pressure at all. It now names both prospection tiers (1 armed + 1 fired = demotions 2), which is the number closing again — the same shape as the 2026-08-01 note that `demotions 1` had stopped meaning what it meant one layer earlier, one layer on.
2026-08-02 | intend (iid 2 armed; one candidate declined) | One new promise, from the open-questions ledger's most repeated entry (asked at store `t=33` and `t=37`, and standing in `README-l6 §4`): a generated item has no chain, no distinct-value count and no set-once status, so `confidence_for` falls through to CERTAIN on it — so every ‰ this new surface prints over generated content would be a certainty nobody computed, and today's census could not see it, every number being 1000 already. Armed at store `t=40` on `kind=attr and entity=1 and key=layer and val>=7`. **Its timing applies the seed's lesson instead of repeating it**: iid 1 arrived one stage late because a shared scorer is written BEFORE a layer is claimed while a condition on a layer can only see the claim, and a dogfood surface is upgraded AFTER a claim — the same mechanism, pointing the other way. **The other candidate is deliberately not armed, and that is the field note**: `R7` clause 7's bequest — must a §4.2 support entry be RECOVERABLE or only INGESTED — is the question this ledger asks most insistently (t=33, t=37, and `BOUNDARY.log` line 34), and it must be settled at Layer 7's **Stage A**, which is before the claim this reading can see. Arming it would knowingly buy a reminder that arrives after the decision it exists to inform, for the one question whose whole value is being early. So the store gets one promise and a recorded refusal, and the refusal is the more useful half: **this reading can watch for a fact and not for the moment before it**, and a promise worth arming is one whose right moment is a fact and not a stage.
2026-08-03 | consolidate (THE GENERATION CENSUS ON REAL FUEL, measured out-of-suite) | The mandatory `[L7]` measurement, and it is a **negative twice over**: under this shell's declared reading **no answer over this project's own history requires composition, and none can carry the `generated` tag**. `COMPOSITION_FORM` determines a `profile` item from two `part` assertions; this reading emits `session_summary`, `attr` and `intend` and **nothing else**, so the two vocabularies are DISJOINT — `count(part)` and `count(profile)` both abstain, `generate(boundary-1-memory)` ABSTAINS, and the lineage ledger holds 0 items at 0 cells. **The reading cannot compose, and that is a property of the READING and not a defect of the layer**: it is exactly the shape of the 2026-08-02 finding that `origin` stands outside this shell's set-once reading — the shell narrows and never widens — and the same engine composes 160 items on `corpora/l7compose`. The emptiness is rendered as **rows and not a silence** (the L6 census precedent, one layer on), and the disjointness is asserted as the CAUSE, so a session that widened the reading goes red rather than quietly changing what the census means. The other half of the same finding is what Layer 7 cost this store: `L7State` adds one field and this fuel gives it nothing to record, so the ledger is empty, the derived state is the Layer-6 state TO THE CELL, and the two canonical bodies differ in exactly two branches — `layer_cap` and an empty `lineage`. Layer 6 added no field because §5.1 L6 SAID SO; Layer 7 adds one and spends nothing because this reading has nothing for it, which is the same identity arrived at by measurement instead of by constitution.
2026-08-03 | consolidate (THE CERTAINTY CENSUS — iid 2's shell half, and the residual is bigger than the promise said) | The promise asked for two things and only one of them can be exercised here, which is itself the measurement. `§4.2` becoming the difference between a correct answer and a zero is honoured in full — every rendered answer now prints its tag. But *"every permille this surface prints over generated content is a certainty nobody computed"* **cannot be exercised at all under this reading**, there being no generated content to price. So what is surfaced is the RESIDUAL ITSELF: every rendered `‰` is classified by the branch of `l6.confidence_for` that produced it — `measured` (a set-once chain, claimants counted), `default:not-set-once` (CERTAIN on the function's FIRST line, before the chain is looked at), `default:no-model` (an op that attaches CERTAIN by construction), `default:no-chain` (a composed item, README-l6 §4's residual proper). On a copy of the real store with this session's own `intend` and `remember` applied (49 stored, 490 derived), over the 9009 answers the report reads back while rendering: **measured 0, not-set-once 7585, no-model 650, no-chain 0.** NOT ONE ‰ THIS SURFACE PRINTS IS MEASURED — the residual README-l6 §4 named for a *generated* item, reached one branch earlier on **every ordinary key**, so it covers the whole surface rather than a corner of it. iid 2's own text predicted exactly half of that and did not know it was the smaller half. It is stated OPEN and not closed: README-l7 §4 says what would close it (a reading on which composition can be WRONG) and inventing one to exercise a level is the thing README-l6 §1.3 warns against. **And the counts moved under the write for the seventh time in this file** — 8770 / 7377 / 633 before the remember — while the finding did not move by one answer, which is the 2026-08-02 lesson about which numbers a claim rests on, holding again.
2026-08-03 | consolidate (THE UNTAGGED DIAGNOSTICS — found by rendering every tag rather than by measuring anything) | Printing the `§4.2` kind beside every answer surfaced a seam nobody went looking for: **six ops answer non-abstaining with `provenance: null`** — `consolidation`, `prospection`, `calibration`, `lineage`, `profile` and `count`, which is every layer's own diagnostic — while `forgetting` carries a valid `absent` tag beside them, so it is an **uneven** seam and not a blanket property of the engine. `§4.2.2` has no exception for a diagnostic, so read literally each of those is an answer that scores **0 however correct it is**, and the report renders eight of them per view. It costs nothing today and the note says so rather than escalating it: **no §5 L7 denominator contains a diagnostic query**, so no gated number moves, nothing frozen is touched and no claim is made about a gate — the surface prints `UNTAGGED` rather than laundering it into a kind, and the trial requires the untagged set to be exactly those six so that an ORDINARY verb losing its tag would be a different finding and could not pass as this one. Where it stops being free is Layer 8: §5 L8 is *"introspection answered FROM STATE via the ordinary query interface"* and README-l7 §5 already records that `{"op":"lineage"}` is that sentence in miniature — so at Layer 8 the untagged class and the layer's own subject are the same class.
2026-08-03 | consolidate --budget (the support-recoverability rate, and why it says nothing) | `R8` clause 5(a) settled `§4.2`'s support reading SHAPE-ONLY — a support entry must be INGESTED, not RECOVERABLE — said the weaker claim out loud and paid for it with an UNGATED recoverability rate reported beside the gated numbers. The shell now reports that rate, and on this store it reads **all-recoverable at every cap tried**: 7 of 7 cited `t` at 4194304, 5000, 3000, 2000, 1200 and 800, where the fold loses from 0 to 125 events. The reason is not a generous budget and is worth writing down because it is `README-l7 §2.3`'s finding arrived at through a different reading: **`current` cites the assertion that ANSWERS it**, so the reading loses the answer before it can lose the warrant — where a chain is shed the answer abstains and there is no citation left to be unrecoverable. So the diagnostic that was paid for to make a weaker claim honest is, on this fuel, uninformative — and the honest thing is to print the rate, say which of the two is the law and which is the diagnostic, and record that no reading this shell has can make it say anything. Asserted at four caps in the trial rather than quoted from one.
2026-08-03 | intend (iid 3 armed; the ledger closed twice first) | The ledger reads **2 fired / 0 pending** and the pending set is empty for the first time since `[L5] [DOGFOOD]`: iid 1 fired at store `t=39`, iid 2 at store `t=46`, and **both `intend` episodes come back byte-exact from `read(t0)` tagged `recall`** — the take-back rule, twice, and `recall` is the only honest tag either can carry because nothing regenerates a fired intention's episode once its pending entry is gone. The trial now asserts BOTH directions at once (fired -> `recall`, pending -> `derive`), which is also why this session's own ritual write could not turn it red the way `[L6]`'s did. **One new promise, and the lateness theorem decided WHICH HALF it carries rather than whether to arm it**: iid 3 at store `t=47` on `kind=attr and entity=1 and key=layer and val>=8`, carrying the UNTAGGED-diagnostic seam to the surface that will have to answer for it. The SHELL half is on time — a dogfood surface is upgraded AFTER a claim, so a claim-conditioned promise lands with it, which is why iid 2 landed and iid 1 did not — while the ENGINE half of the same question (*must a diagnostic answer carry a tag?*) would be settled at Layer 8's **Stage A**, before the claim this reading can see, so it is deliberately NOT what this promise says, exactly as 2026-08-02 refused to arm `R7` clause 7's bequest for the same reason. The rule has now been applied three times and is worth stating as one: **a promise worth arming is one whose right moment is a fact this reading can assert, and its payload must be the half that arrives with the fact.**
